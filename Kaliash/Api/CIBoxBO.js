/**
 * curdCIboxes
 * PingCIBoxes
 * ApplyNewCIBoxes
 * 
 */

const ping = require("net-ping");

class CIBoxBO{
    constructor(){

    }

    CookCIBox(repo, branch, commit){
    }

    PingCIBox(boxid){
        // 直接调用ping

        box = EO.getEnvByID()
        if box.type != ..box
        
        session = ping.session
        session.pingHost(box.ip, (error, target, sent, rcvd) => {
            if (error)
            else{
                
            }
        })
    }

}